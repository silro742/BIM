# Git 사용설명서 (초보자용)

이 문서는 Windows PowerShell 기준으로, 프로그래밍을 처음 접하는 분도 따라 할 수 있도록 정리한 Git 안내서입니다.

## 1. Git이 무엇인가요?

Git은 파일이 바뀐 **모든 이력을 기록**해주는 프로그램입니다. "언제, 무엇을, 왜 바꿨는지"를 저장해두기 때문에:

- 실수로 파일을 망가뜨려도 이전 상태로 되돌릴 수 있고
- 여러 사람이 같은 파일을 동시에 작업해도 충돌 없이 합칠 수 있고
- GitHub 같은 사이트에 올려서 어디서나 같은 코드를 받아볼 수 있습니다.

## 2. 꼭 알아야 할 용어 5가지

| 용어 | 뜻 |
|---|---|
| **저장소 (Repository, repo)** | 파일들과 그 변경 이력이 저장되는 폴더 |
| **커밋 (Commit)** | "지금까지 바뀐 내용을 이 시점으로 저장한다"는 하나의 스냅샷 |
| **브랜치 (Branch)** | 원본을 건드리지 않고 따로 작업할 수 있는 갈래길. 기본 브랜치는 보통 `main` |
| **원격 저장소 (Remote / origin)** | GitHub처럼 인터넷에 있는 저장소. 내 컴퓨터의 저장소와 짝을 이룹니다 |
| **클론 (Clone)** | 원격 저장소를 내 컴퓨터로 통째로 복사해오는 것 |

## 3. 설치 확인

이미 설치되어 있다면 아래 명령으로 버전이 나옵니다.

```powershell
git --version
```

`git version 2.xx.x.windows.x` 같은 결과가 나오면 준비 완료입니다. 설치가 안 되어 있다면 https://git-scm.com/downloads/win 에서 받으시면 됩니다.

### 최초 1회만 하는 설정

Git으로 커밋할 때 "누가" 바꿨는지 기록하기 위해 이름과 이메일을 등록해야 합니다.

```powershell
git config --global user.name "본인 이름"
git config --global user.email "본인 이메일"
```

## 4. 자주 쓰는 명령어 모음 (치트시트)

| 명령어 | 하는 일 |
|---|---|
| `git clone <주소>` | 원격 저장소를 통째로 내 컴퓨터에 복사 |
| `git status` | 지금 무엇이 바뀌었는지 확인 (가장 많이 쓰는 명령어) |
| `git add <파일명>` | 커밋에 포함할 파일을 선택(스테이징) |
| `git add .` | 바뀐 파일 전부 선택 (주의: 아래 5번 참고) |
| `git commit -m "설명"` | 선택한 파일들을 하나의 저장 시점으로 기록 |
| `git push` | 내 컴퓨터의 커밋을 GitHub(원격 저장소)로 올리기 |
| `git pull` | GitHub의 최신 내용을 내 컴퓨터로 받아오기 |
| `git log --oneline` | 지금까지의 커밋 기록을 한 줄씩 보기 |
| `git branch` | 브랜치 목록 확인 |
| `git checkout <브랜치명>` | 다른 브랜치로 이동 |
| `git diff` | 실제로 어느 줄이 바뀌었는지 자세히 보기 |

## 5. 실전 흐름 — 처음부터 끝까지

### (1) 저장소 받기 (최초 1회)

```powershell
cd $env:USERPROFILE\Documents
git clone https://github.com/silro742/BIM.git
cd BIM
```

- `$env:USERPROFILE`은 PowerShell이 자동으로 내 사용자 폴더 경로로 바꿔주는 변수라, `<사용자명>` 같은 걸 직접 쓸 필요가 없습니다.
- 이미 받아둔 폴더가 있다면 이 단계는 건너뛰고, 그 폴더로 `cd`만 하면 됩니다.

### (2) 파일 수정

원하는 방식(메모장, 코드 에디터, Claude Code 등)으로 파일을 수정합니다.

### (3) 무엇이 바뀌었는지 확인

```powershell
git status
```

빨간색으로 "modified" 또는 "new file"이라고 나오면 아직 커밋 전 상태입니다.

### (4) 커밋에 포함할 파일 선택

```powershell
git add 파일이름.txt
```

여러 개면 이어서 여러 번 입력하거나, 전부 다 넣으려면:

```powershell
git add .
```

> ⚠️ **주의**: `git add .`는 폴더 안의 바뀐 파일을 전부 넣습니다. 비밀번호, API 키가 담긴 파일(`.env` 등)이 섞여 있지 않은지 `git status`로 먼저 확인하는 습관을 들이세요.

### (5) 커밋 (저장 시점 기록)

```powershell
git commit -m "무엇을 왜 바꿨는지 짧게 설명"
```

커밋 메시지는 나중에 이력을 볼 때 "이게 뭐였더라?" 를 알려주는 유일한 단서이니, `"수정함"` 같은 말보다는 `"표고점 추출 버그 수정"`처럼 구체적으로 씁니다.

### (6) GitHub에 올리기 (push)

```powershell
git push
```

처음 올리는 브랜치라면 아래처럼 목적지를 지정해야 할 수도 있습니다:

```powershell
git push -u origin 브랜치이름
```

한 번 `-u`로 연결해두면, 다음부터는 그냥 `git push`만 쳐도 됩니다.

### (7) 다른 컴퓨터/다른 세션에서 최신 내용 받기

```powershell
git pull
```

## 6. 브랜치, 왜 나눠서 작업하나요?

`main` 브랜치는 보통 "완성되고 안정적인" 버전만 두는 곳입니다. 새로운 기능을 시험해볼 땐 별도 브랜치를 만들어 작업하고, 문제없으면 나중에 합칩니다(병합/PR).

```powershell
git checkout -b 새브랜치이름   # 브랜치를 만들면서 바로 이동
git checkout main             # main으로 돌아가기
```

## 7. 자주 만나는 오류와 해결법

| 오류 메시지 | 원인 | 해결법 |
|---|---|---|
| `'git'은(는) 내부 또는 외부 명령... 인식되지 않습니다` | Git이 설치 안 됐거나 PATH 미등록 | Git 재설치 또는 새 터미널 창 열기 |
| `경로에 잘못된 문자가 있습니다` (cd 명령) | `<사용자명>` 같은 예시 텍스트를 그대로 입력함 | 실제 경로로 바꾸거나 `$env:USERPROFILE` 사용 |
| `destination path '...' already exists and is not an empty directory` | 그 이름의 폴더가 이미 있음(보통 clone 위치를 잘못 지정) | 다른 폴더로 이동해서 다시 clone, 또는 기존 폴더 확인 후 삭제 |
| `fatal: not a git repository` | git 폴더가 아닌 곳에서 git 명령을 실행함 | 저장소 폴더로 `cd` 이동했는지 확인 |
| push 시 인증 요청 | GitHub 로그인 정보 필요 | 브라우저 로그인 창이 뜨면 로그인, 또는 GitHub Desktop/Personal Access Token 사용 |
| merge conflict (충돌) | 같은 줄을 서로 다르게 고친 두 커밋을 합칠 때 발생 | 파일을 열어 `<<<<<<<` `=======` `>>>>>>>` 표시 사이를 직접 정리 후 다시 커밋 |

## 8. 절대 무심코 쓰면 안 되는 명령어

아래 명령들은 **되돌리기 어렵거나 불가능**합니다. 뜻을 정확히 알기 전에는 실행하지 마세요.

- `git reset --hard` — 커밋하지 않은 변경사항을 전부 버립니다.
- `git push --force` — 원격 저장소의 이력을 강제로 덮어씁니다. 다른 사람의 작업을 지울 수 있습니다.
- `git clean -f` — 커밋되지 않은 새 파일을 전부 삭제합니다.
- `git checkout -- 파일명` / `git restore 파일명` — 그 파일의 저장 안 된 수정 내용을 버리고 마지막 커밋 상태로 되돌립니다.

## 9. 요약 — 이것만 기억하면 됩니다

```
1. git status       ← 지금 상태 확인 (아무 때나 실행해도 안전)
2. git add .        ← 바뀐 파일 선택
3. git commit -m "설명"   ← 저장 시점 기록
4. git push         ← GitHub에 올리기
```

이 4줄만 순서대로 익히면 일상적인 작업의 90%는 해결됩니다. 나머지(브랜치, 충돌 해결 등)는 필요할 때 하나씩 찾아보면 됩니다.
