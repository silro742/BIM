# 브랜치 통합안

작성일 2026-08-25 · 대상 `silro742/BIM` · 조사 기준 커밋 `7d39a00`

---

## 1. 현황

`main` 은 2026-03-18 에서 멈춰 있고 파일이 1개뿐입니다.
실제 작업물은 8-24 밤 ~ 8-25 새벽 사이에 만들어진 **7개 브랜치에 흩어져** 있습니다.

모든 브랜치가 `main`(`06c08b3`)에서 직접 갈라졌고, 서로 병합된 적이 없습니다.

| 브랜치 | 팁 | 시각(KST) | 내용 |
|---|---|---|---|
| `main` | `06c08b3` | 03-18 09:14 | 지형분석 앱만 |
| `complete-all-tasks-gujthr` | `d50c7d7` | 08-24 23:22 | 06 앱 v1.1.0 + 감사보고 |
| `markdown-html-app-review-t00907` | `a751267` | 08-25 00:21 | 06 앱 v1.1.1 + 감사보고 확장 |
| `html-app-upgrade-plan-6cd7yu` | `3312115` | 08-25 03:59 | **07 배수 앱 v2.3.1** + 계획서 |
| `html-app-upgrade-plan-an89l6` | `09d5d45` | 08-25 04:15 | 06 계획서 v1.0 |
| `standard-cross-section-app-upgrade-bzsi6o` | `1a5c2f8` | 08-25 05:51 | **06 앱 v1.2.0** + 계획서 v1.1 |
| `korean-question-3mibjg` | `1dd8761` | 08-25 06:34 | 5개 브랜치 백업 사본 |
| `html-app-upgrade-plan-fuhq5b` | `d26c001` | 08-25 07:40 | **06 앱 v1.1.0(별개 계보)** + 계획서 v2.0 |

## 2. 핵심 문제 — 06 앱이 두 갈래로 갈라졌다

06 앱 사본이 4개 있는데, **하나의 직선이 아니라 두 계보**입니다.

### 계보 A — 감사·수정 라인

```
gujthr  v1.1.0  238,161 bytes  3,636행   08-24 23:22
   ↓  재감사 10건 수정
t00907  v1.1.1  242,922 bytes  3,680행   08-25 00:21
   ↓  P0 구현 (F-01 / F-02 / F-03)
bzsi6o  v1.2.0  315,529 bytes  4,843행   08-25 05:51
```

### 계보 B — 독립 업그레이드 라인

```
로컬 v1.0.1 (3,305행)
   ↓  물량 정확성 · 반출 게이트 · 핸드오프 메타
fuhq5b  v1.1.0  234,221 bytes  3,603행   08-25 07:40
```

계획서에 근거가 남아 있습니다.

> `fuhq5b` 계획서 v2.0 — **대상**: `06 표준횡단과 횡단토공 v1.0.1` (단일 파일 HTML, 3,305행)

즉 `fuhq5b` 세션은 계보 A 의 존재를 모른 채 **더 오래된 로컬 원본에서 새로 출발**했습니다.

### 두 계보는 서로를 포함하지 않는다

키워드 출현 횟수로 확인한 결과입니다.

| 키워드 | gujthr | t00907 | bzsi6o | fuhq5b |
|---|---:|---:|---:|---:|
| `반출` | 6 | 6 | 7 | **13** |
| `물량` | 15 | 16 | 19 | **24** |
| `F-01` | 0 | 0 | **10** | 1 |
| `F-02` | 0 | 0 | **10** | 3 |
| `F-03` | 0 | 0 | **8** | 0 |
| `v1.2.0` | 0 | 0 | **5** | 0 |
| `v1.1.1` | 0 | 18 | 16 | 0 |

`fuhq5b` 에만 있는 용어 — `반출조건`, `반출차단`, `반출을`, `반출이` 는 `bzsi6o` 에 **0회**입니다.
반대로 `bzsi6o` 의 `F-03` 은 `fuhq5b` 에 **0회**입니다.

**결론: 어느 한쪽을 고르면 다른 쪽 작업이 통째로 사라집니다.**

### 부수 문제 — 버전 번호 충돌

`gujthr` 의 `v1.1.0`(3,636행)과 `fuhq5b` 의 `v1.1.0`(3,603행)은 **같은 번호, 다른 내용**입니다.
통합 시 버전 체계를 다시 잡아야 합니다.

## 3. 충돌 없는 항목

| 대상 | 위치 | 상태 |
|---|---|---|
| 07 배수 관망설계 앱 v2.3.1 (408KB) | `6cd7yu` 단독 | 충돌 없음 |
| 07 계획서 (33KB) | `6cd7yu` 단독 | 충돌 없음 |
| 지형분석 앱 | 모든 브랜치 동일 (54,953 bytes) | 충돌 없음 |
| `.claude/`, `tools/`, `docs/로컬복귀_가이드.md` | `byeoli-team-location-vjvy0p` 단독 | 충돌 없음 |

06 계획서는 3종이며 계보에 대응합니다.

| 문서 | 브랜치 | 성격 |
|---|---|---|
| 계획서 v1.0 (43KB) | `an89l6` | 계보 A 초판 |
| 계획서 v1.1 (61KB) | `bzsi6o` | 계보 A 개정판 (P0 구현 완료 표기) — v1.0 을 대체 |
| 계획서 v2.0 (22KB) | `fuhq5b` | 계보 B 전용 |

## 4. 선행 조건 — 로컬 원본 확인이 먼저다

**저장소의 어떤 사본도 최신이 아닐 수 있습니다.**

모든 사본은 8-25 새벽 세션들이 올린 것이고, 그 뒤로 사용자가 로컬 토성 폴더에서
계속 작업했다면 로컬본이 가장 앞섭니다. 통합 기준을 저장소에서 정하면 로컬 작업을 덮어씁니다.

그래서 **1단계는 반드시 로컬 확인**입니다. `tools\01_진단_벼리팀찾기.ps1` 의 `[7]` 항목이
토성 폴더의 실제 구조와 파일 목록을 출력합니다. 각 앱 파일의 `<title>` 버전을 확인해
저장소 사본들과 대조한 뒤에 기준을 정합니다.

## 5. 목표 구조

**저장소 구조는 로컬 토성 폴더 구조를 그대로 따릅니다.**
저장소가 다른 구조를 강요하면, 토성 폴더를 연결했을 때 전부 이동·충돌로 나타납니다.

`bzsi6o` 브랜치와 계획서 본문의 경로 표기(`...\토성\06_표준횡단_횡단토공`)로 보아
**앱별 폴더 방식**이 유력합니다. 1단계에서 확정합니다.

```
main
├── README.md                       프로젝트 개요와 앱 목록
├── BIM_Slope_EL_Automation.html    (기존 위치 유지 — 경로 변경은 이력 단절 위험)
├── 06_표준횡단_횡단토공/
│   ├── 06_표준횡단_횡단토공.html
│   └── 06_표준횡단_횡단토공_업그레이드_계획서.md
├── 07_배수_관망설계/
│   ├── 07_배수_관망설계.html
│   └── 07_배수관망_업그레이드_계획서.md
├── docs/
│   ├── 06_표준횡단_감사보고.md
│   ├── 로컬복귀_가이드.md
│   └── 브랜치_통합안.md
├── tools/
└── .claude/
```

## 6. 단계별 통합 절차

### 1단계 — 로컬 실측 (사용자 실행)

```powershell
powershell -ExecutionPolicy Bypass -File .\01_진단_벼리팀찾기.ps1
```

확인할 것: 토성 폴더 구조, 06·07 앱의 `<title>` 버전, 최종 수정 시각.
결과를 알려주시면 기준본과 최종 구조를 확정합니다.

### 2단계 — 무충돌 통합 (즉시 실행 가능)

`main` 에서 통합 브랜치를 만들고 다음을 모읍니다. 충돌이 없어 안전합니다.

- `6cd7yu` → 07 앱 v2.3.1 + 07 계획서
- `byeoli-team-location-vjvy0p` → `.claude/`, `tools/`, 가이드 문서
- `t00907` → 06 감사보고 (7.3KB 확장판)
- `bzsi6o` → 06 계획서 v1.1 (v1.0 을 대체하므로 `an89l6` 은 흡수)

이 시점에서 07 앱과 도구·문서가 `main` 계열에 올라옵니다.

### 3단계 — 06 앱 포크 해소 (판단 필요)

세 가지 선택지입니다.

| 안 | 방식 | 장점 | 단점 |
|---|---|---|---|
| **A (권장)** | `bzsi6o` v1.2.0 을 기준으로 `fuhq5b` 의 반출 게이트·물량 정확성 작업을 이식 → **v1.3.0** | 양쪽 작업 모두 보존 | 이식·검증 작업 필요 |
| B | `bzsi6o` v1.2.0 만 채택 | 즉시 완료 | `fuhq5b` 의 반출 게이트 소실 |
| C | `fuhq5b` 만 채택 | 즉시 완료 | v1.1.1 감사 수정 10건과 P0 3건 소실 |

**A 안을 권합니다.** 두 계보 모두 실제 기능이 들어 있고, 어느 쪽도 버릴 이유가 없습니다.

A 안 실행 순서:

1. `bzsi6o` v1.2.0 을 작업 기준으로 복사
2. `fuhq5b` 와 계보 공통 조상(로컬 v1.0.1 상당) 사이의 차이를 추출
3. 반출 게이트 / 물량 정확성 / 핸드오프 메타 변경분을 v1.2.0 위에 이식
4. 브라우저에서 실제 동작 확인 — 단일 파일 HTML 이므로 자동 테스트가 없어 **수동 검증이 필수**
5. `v1.3.0` 으로 표기하고 변경 이력 정리

이식 대상 후보(키워드 기준): `반출조건`, `반출차단`, `반출을`, `반출이`, 물량 산정 관련 9곳 증가분.
실제 범위는 3단계 착수 시 코드를 직접 대조해 확정합니다.

### 4단계 — main 반영

통합 브랜치를 `main` 에 병합합니다. PR 을 만들지 여부는 지시해 주시면 따르겠습니다.

### 5단계 — 브랜치 정리

`korean-question-3mibjg` 에 5개 브랜치의 파일 사본이 이미 백업되어 있습니다.
통합이 끝나 `main` 이 모든 내용을 담은 뒤에 정리합니다.

| 브랜치 | 처리 |
|---|---|
| `gujthr`, `t00907`, `an89l6`, `bzsi6o`, `fuhq5b`, `6cd7yu` | `main` 반영 확인 후 삭제 |
| `korean-question-3mibjg` | 백업 브랜치 — 당분간 보존 |
| `byeoli-team-location-vjvy0p` | `main` 반영 후 삭제 |

**삭제는 `main` 이 모든 파일을 담고 있음을 확인한 뒤에만 합니다.**
브랜치를 지우면 그 커밋 이력은 함께 사라집니다.

## 7. 위험 요소

| 위험 | 대응 |
|---|---|
| 로컬본이 저장소보다 최신 | 1단계 실측을 반드시 선행 |
| 06 앱 이식 중 기능 파손 | 단일 파일 HTML 은 자동 테스트가 없어 브라우저 수동 검증 필수 |
| 버전 번호 충돌 (v1.1.0 두 개) | 통합본을 v1.3.0 으로 올려 혼선 차단 |
| 브랜치 삭제 시 이력 소실 | `main` 반영 확인 후에만 삭제 |
| 파일 경로 변경 | 로컬 토성 구조에 맞춤. 지형분석 앱은 기존 위치 유지 |

## 8. 지금 필요한 결정

1. **06 앱 포크** — A / B / C 중 어느 안으로 갈지 (권장: A)
2. **1단계 진단 결과** — 토성 폴더 구조와 로컬 앱 버전
3. **PR 생성 여부** — `main` 병합을 PR 로 할지 직접 병합할지

1번과 3번은 지금 답해 주실 수 있고, 2번은 PC 에서 스크립트를 돌리셔야 합니다.
**2단계(무충돌 통합)는 답을 기다리지 않고 바로 실행 가능합니다.**
