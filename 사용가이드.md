# BIM Slope & EL Automation 사용가이드 (초보자용)

경사도(Slope) 산출과 표고(EL, Elevation) 분석을 웹 브라우저에서 바로 해볼 수 있는 도구입니다. Civil 3D나 Revit이 없어도 원리를 먼저 익히고, 실제 프로젝트에 쓸 스크립트까지 뽑아낼 수 있습니다.

## 1. 실행 방법

설치할 프로그램이 없습니다. 파일 하나만 열면 됩니다.

1. `BIM_Slope_EL_Automation.html` 파일을 찾습니다.
2. 마우스 오른쪽 클릭 → **연결 프로그램** → **Chrome**(또는 Edge, Firefox 등 브라우저) 선택
   - 또는 그냥 더블클릭해도 기본 브라우저로 열립니다.
3. 인터넷 연결이 없어도 동작합니다 (전부 파일 안에서 계산되는 프로그램입니다).

## 2. 화면 구성

| 위치 | 설명 |
|---|---|
| 왼쪽 메뉴 | 1~7단계 기능 목록. 클릭해서 단계를 이동합니다 |
| 가운데 캔버스 | 지형(지표면)이 그림으로 표시되는 곳. 여기를 클릭해서 점을 찍습니다 |
| 오른쪽 패널 | 선택한 단계의 버튼과 결과가 표시되는 곳 |
| 오른쪽 위 **Load Sample Surface** 버튼 | 연습용 가상 지형을 생성합니다 |

## 3. 시작하기 — 지형 불러오기

화면을 처음 열면 "No Surface Loaded"라고 나옵니다. 아직 분석할 지형이 없다는 뜻입니다.

- 화면 가운데 **Generate Sample Terrain** 버튼(또는 오른쪽 위 **Load Sample Surface**)을 클릭하세요.
- 가상의 지형 데이터가 자동으로 만들어지고, 캔버스에 지형이 그려집니다.

> 실제 현장 측량 데이터를 불러오는 기능은 없고, 이 도구는 **연습·검증용 샘플 지형**으로만 동작합니다.

## 4. 단계별 사용법

왼쪽 메뉴에서 원하는 단계를 클릭해 이동합니다. 모든 단계는 **지형을 불러온 뒤에만** 사용할 수 있습니다.

### Step 1. 표고점 추출 (Surface Elevation Extraction)

지형 위의 특정 지점 높이(표고)를 확인합니다.

1. 캔버스(지형 그림) 위를 클릭해서 확인하고 싶은 지점을 하나 이상 찍습니다.
2. **Extract Elevation** 버튼 클릭.
3. 오른쪽 패널에 각 지점의 표고값(EL)이 표로 나옵니다.
4. 다시 하려면 **Clear** 버튼으로 초기화합니다.

### Step 2. 경사도 산출 (Slope Calculation & Direction Arrow)

두 지점 사이의 경사(기울기)를 계산합니다.

1. 캔버스에서 시작점(Point A)을 클릭.
2. 도착점(Point B)을 클릭.
3. 필요하면 **Use 3D distance** 체크박스로 계산 방식을 바꿉니다 (평면 거리 대신 실제 경사면 거리 사용).
4. **Calculate Slope** 버튼 클릭 → 두 점 사이의 경사도(%)와 방향이 표시됩니다.

### Step 3. 워터 드롭 분석 (Water Drop Path Analysis)

빗물이 지형을 따라 어디로 흘러가는지 추적합니다 (Civil 3D의 "워터 드롭" 기능과 같은 원리).

1. 캔버스에서 물을 떨어뜨릴 지점(들)을 클릭.
2. **Trace Water Drop** 버튼 클릭 → 경사를 따라 흘러내리는 경로가 그려집니다.

### Step 4. 표면 분석 (Surface Analysis)

지형 전체를 표고·경사 구간별로 색칠해서 한눈에 보여줍니다.

1. 별도로 점을 찍을 필요 없이 **Run Surface Analysis** 버튼만 클릭.
2. 표고 단계별 색상 구간과 경사 화살표가 표시됩니다.

### Step 5. 유역면적 (Catchment Area)

특정 지점으로 빗물이 모이는 범위(유역)를 계산합니다.

1. 캔버스에서 물이 모이는 기준점(Pour Point)을 클릭.
2. **Delineate Catchment** 버튼 클릭 → 해당 지점으로 모이는 유역 범위가 표시됩니다.

### Step 6. 법규 검토 (Regulations & Compliance)

계산한 경사도가 국가별·용도별 최소/최대 기준을 만족하는지 확인합니다.

1. **Slope (%)** 입력란에 검토할 경사값을 입력.
2. **Surface Type**에서 용도 선택: 도로(Road) / 일반 지표면(General) / 무장애 경사로(Barrier-free Ramp) / 배수관(Pipe Drainage).
3. **Country**에서 기준 국가 선택: 한국 / 싱가포르 / 미국 / 국제 기준 / 전체.
4. **🛡️ Check Compliance** 버튼 클릭 → 통과(PASS) 여부와 부족한 경우 개선 권고사항이 나옵니다.
5. 화면 아래 **Regulation Database**에서 적용 가능한 규정 원문도 확인할 수 있습니다.

### Step 7. 통합 스크립트 생성 (Integration Script Generation)

지금까지의 분석 로직을 실제 소프트웨어에서 쓸 수 있는 코드로 내려받습니다.

1. 플랫폼 카드 중 하나 선택:
   - **Dynamo (Revit)** — Python
   - **Revit C# Add-in** — C#
   - **Civil 3D Python** — Python
   - **Civil 3D C#** — C#
2. 코드 미리보기가 화면에 표시됩니다.
3. **⬇️ Download** 버튼으로 파일을 저장해 실제 프로젝트에서 활용합니다.

## 5. 자주 묻는 질문

**Q. 실제 측량 파일(dwg, csv 등)을 불러올 수 있나요?**
A. 아니요. 이 도구는 샘플 지형으로 원리를 연습하고 스크립트를 뽑아내는 용도입니다. 실제 데이터 적용은 Step 7에서 받은 스크립트를 Civil 3D/Revit/Dynamo에서 실행해야 합니다.

**Q. 인터넷이 없어도 되나요?**
A. 네. 파일 하나로 브라우저 안에서만 동작합니다.

**Q. 작업하던 내용이 저장되나요?**
A. 아니요. 브라우저 창을 닫거나 새로고침하면 입력했던 점·결과가 모두 사라집니다. 필요한 결과는 미리 캡처하거나 Step 7의 스크립트 다운로드로 남겨두세요.

**Q. 다른 단계로 이동하면 이전 단계 데이터가 사라지나요?**
A. 각 단계별 입력(점, 결과)은 그 단계 안에서 유지됩니다. 다만 **Load Sample Surface**로 지형을 새로 불러오면 모든 단계의 기록이 초기화됩니다.

## 6. 주의사항

- 이 도구가 산출하는 값은 **가상 샘플 지형** 기준입니다. 실제 설계·시공 판단에는 실제 측량 데이터와 정식 소프트웨어(Civil 3D 등) 검토가 반드시 필요합니다.
- Step 6의 법규 검토는 참고용 데이터베이스이며, 실제 인허가 기준은 관련 법규 원문과 담당 기관 확인을 우선해야 합니다.
